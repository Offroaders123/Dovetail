{
  "name": "dovetail",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vite --host --port=5500",
    "build": "npx tsc; npx vite build"
  },
  "dependencies": {
    "@stardazed/streams-compression": "^1.0.0",
    "nbtify": "^1.20.1"
  },
  "devDependencies": {
    "better-typescript": "^0.0.7",
    "new-javascript": "^0.2.4"
  }
}